### Options

`-w n` instructs the CLI to generate a usage document formatted to n columns.
This option is only honored on unix-like systems (not supported on Windows).

`-i` generates an extra line to insert a hugo include shortcode at the end of the usage doc.

This is used by go-swagger maintainers to update the documentation site.
