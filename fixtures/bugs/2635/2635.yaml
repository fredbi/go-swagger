swagger: '2.0'
info:
  title: Example
  version: 1.0.0
schemes:
  - http
produces:
  - application/json
responses: {}
paths:

  /send_email:
    post:
      operationId: operation
      parameters:
        - name: params
          in: body
          required: true
          schema:
            $ref: "#/definitions/Model"
      responses:
        200:
          description: "Success"

definitions:
  Model:
    type: object
    description: "An object to hold the email parameters for calls"
    required:
      - template_data
    properties:
      template_data:
        additionalProperties:
          type: object
