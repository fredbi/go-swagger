swagger: '2.0'
info:
  version: 0.0.0
  title: PrimitiveEnum
  description: |
    Enum generation with distinguished type for primitive enum
  contact:
    name: API Support
    url: http://www.example.com
    email: contact@github.com
consumes:
  - application/json
produces:
  - application/json
paths:
  /fixture:
    get:
      responses:
        200:
          description: OK
definitions:
  enum_type_one: { type: string, enum: [ '1' ] }

  included:
    type: array
    items:
      type: object
      properties:
        ida: { type: string, enum: [ '1' ], x-go-enum-type: true }
        idb: { $ref: '#/definitions/enum_type_one' }
