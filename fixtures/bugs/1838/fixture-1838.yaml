swagger: "2.0"
basePath: /
schemes:
  - http
consumes:
  - application/json
produces:
  - application/json

paths:
  /upload:
    post:
      tags:
      - uploads
      summary: uploads
      operationId: upload#File
      consumes:
      - multipart/form-data
      parameters:
      - name: file
        in: formData
        type: file
        required: true
      responses:
        "200":
          description: OK

