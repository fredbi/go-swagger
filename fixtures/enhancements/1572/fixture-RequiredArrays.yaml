swagger: '2.0'
info:
  version: "1.0.0"
  title: 'issue 1232'
  description: verifies marshalling code on discriminated type
produces:
  - application/json
consumes:
  - application/json
paths:
  /sanity:
    get:
      operationId: getSanity
      description: no description
      parameters:
      - name: sanityParam
        in: body
        schema:
          $ref: '#/definitions/objectWithRequiredArray'
      responses:
        default:
          description: Generic Out
          schema:
            $ref: '#/definitions/objectWithRequiredArray'
definitions:
  objectWithRequiredArray:
    type: object
    required:
    - prop1
    properties:
      prop1:
        type: array
        items:
          type: integer
          maximum: 100
      prop2:
        type: array
        items:
          type: integer
          maximum: 100
      prop3:
        type: array
        minItems: 1
        items:
          type: integer
          maximum: 100
