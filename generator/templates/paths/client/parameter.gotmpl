{{/* clientParameterTarget is where the parameters section writes what clientParameter renders. */}}
{{ define "clientParameterTarget" }}{{ joinFilePath .Target (toPackagePath .ClientPackage) (toPackagePath .Package) }}{{ end }}

{{/* clientParameterFileName is the name of the file it writes. */}}
{{ define "clientParameterFileName" }}{{ (snakize (pascalize .Name)) }}_parameters.go{{ end }}
