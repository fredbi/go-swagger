{{/* clientResponseTarget is where the responses section writes what clientResponse renders. */}}
{{ define "clientResponseTarget" }}{{ joinFilePath .Target (toPackagePath .ClientPackage) (toPackagePath .Package) }}{{ end }}

{{/* clientResponseFileName is the name of the file it writes. */}}
{{ define "clientResponseFileName" }}{{ (snakize (pascalize .Name)) }}_responses.go{{ end }}
