{{/* serverAutoconfigureapiTarget is where the autoconfigure section writes what serverAutoconfigureapi renders. */}}
{{ define "serverAutoconfigureapiTarget" }}{{ joinFilePath .Target (toPackagePath .ServerPackage) }}{{ end }}

{{/* serverAutoconfigureapiFileName is the name of the file it writes. */}}
{{ define "serverAutoconfigureapiFileName" }}auto_configure_{{ (snakize (pascalize .Name)) }}.go{{ end }}
